#include <bits/stdc++.h>
using namespace std;

int main() {
    long long x, y;

    cin >> x >> y;

    if (x > y) {
        cout << x << "\n"s;
    } else {
        cout << y << "\n"s;
    }

    return 0;
}