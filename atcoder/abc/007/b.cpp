#include <bits/stdc++.h>
using namespace std;

int main() {
	string A;
	cin >> A;

	if (A[0] == 'a' && A.size() == 1) {
		cout << -1 << endl;
	} else {
		cout << 'a' << endl;
	}

	return 0;
}